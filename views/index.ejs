<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Wise Human</title>
    <link rel="stylesheet" href="/styles/main.css" />
  </head>
  <body>
    
    <video autoplay muted loop id="video-background">
      <!-- <source src="../public/vd-1.mp4" type="video/mp4"> -->
      Your browser does not support the video tag.
    </video>

    <div class="container">
      <div class="advice">
        <h1>Advice of the day</h1>
        <% if (data.responseAdv) { %>
          <div class="advice-container">
            <p><%= data.responseAdv %></p>
          </div>
        <% } else { %>
          <p><%= data.firstAdv %></p>
        <% } %>
        <div class="advice-container">
          <form action="/getRandomAdvice" method="POST">
            <label for="advice">
              <button id="advice" type="submit">Random Advice</button>
            </label>
          </form>
        </div>
      </div>
    </div>
    

  </body>
</html>

